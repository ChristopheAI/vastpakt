# Loop 28 - Check Intro Tone

## User Observation

The user noticed this public line under `Waar gaat tijd verloren?`:

```text
Geen AI-score. Geen besparingsbelofte.
```

## Verdict

Correct observation, and the public copy should change.

The sentence was added as a trust boundary, but on the page it pulls attention toward the wrong comparison: AI-score / savings promise. The section should stay inside the reader's workday language: zoeken, overtypen, rondvragen, tijdverlies.

## Decision

Replace the defensive boundary with a direct description of the check:

```text
Vijf herkenbare signalen om te zien waar zoeken, overtypen of rondvragen telkens terugkomt.
```

## Classification

- `zoeken`, `overtypen`, `rondvragen`: market/workday language from loop 21 and loop 23.
- `Vijf herkenbare signalen`: light edit; describes the current check without introducing a score frame.
- Removed `AI-score`: internal contrast only, not useful public copy here.
- Removed `besparingsbelofte`: protective boundary, but too meta for this section.

## Remaining Risk

The check still needs to avoid implying measured savings. The BAVAST proof below it carries a real practice result, so the intro does not need to repeat a promise boundary.
