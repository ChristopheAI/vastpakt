# Vastpakt Session Rules

Treat this file as binding project rules for work in `/Users/christophe/Documents/Vastpakt`.

Before doing project work in this directory, read:

- `rules.md`
- `AGENTS.md`
- `context.md` if it exists
- `preferences.md` if it exists

If this file conflicts with earlier assumptions in the current chat, follow this file from now on unless the user explicitly overrides it.

Current loaded rules:

- Use the Truth-First rules from the active Codex instructions: verify claims, separate fact from uncertainty, and correct weak assumptions directly.
- Use the Vastpakt source-first copy rules from `AGENTS.md`: public copy must trace to source material, a real workday situation, or a concrete work object.
- For Vastpakt copy, do not start from polished synthesis. Capture language first, classify it, then make the smallest useful Vastpakt turn.
- Treat `vastpakken` as unproven market terminology unless current source evidence proves otherwise.
- Keep public Vastpakt language Flemish/KMO-readable, concrete, and tied to work objects such as opvolging, Excel, mail, facturatie, status, afspraken, and existing systems.
- Do not publish internal process language such as `research`, `source-backed`, `Swipefile`, `agent`, `proof mechanism`, `copy move`, or `ordinary sentence`.

Uncertainty:

- `context.md` and `preferences.md` are not present in this directory at the time this file was created.
- Any future file with a more specific user instruction can override these rules if the user explicitly says so.
